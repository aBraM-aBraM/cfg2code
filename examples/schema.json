{
    "type": "object",
    "properties": {
        "name": {
            "type": "string"
        },
        "age": {
            "type": "integer",
            "minimum": 0,
            "maximum": 99
        },
        "id": {
            "type": "string",
            "pattern": "^[0-9]{9}$"
        }
    },
    "required": ["name", "age", "id"]
}